# 🚀 Quick Start - Testnet Trading Bot

Быстрый запуск торгового бота на Bybit testnet за 5 минут.

## Шаг 1: Получите Bybit Testnet API ключи

1. Зарегистрируйтесь: https://testnet.bybit.com
2. User Center → API Management → Create New Key
3. Права: ✅ Read/Write для Order, Position, Account
4. Сохраните `API Key` и `API Secret`

**💰 Бонус**: На testnet аккаунт автоматически пополняется виртуальными USDT.

---

## Шаг 2: Настройте Telegram (опционально, 2 минуты)

### 2.1 Создайте бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте: `/newbot`
3. Следуйте инструкциям
4. Сохраните токен: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

### 2.2 Получите Chat ID

**Для личных сообщений:**
```bash
# 1. Отправьте любое сообщение вашему боту (например: /start)
# 2. Выполните (замените <TOKEN>):
curl https://api.telegram.org/bot<TOKEN>/getUpdates

# 3. Найдите: "chat":{"id":123456789}
# Это ваш chat_id
```

**Для группы:**
1. Создайте группу в Telegram
2. Добавьте бота в группу
3. Сделайте бота администратором (Settings → Administrators → Add)
4. Отправьте сообщение в группу
5. Выполните команду выше
6. Chat ID группы будет отрицательным: `-1001234567890`

---

## Шаг 3: Клонируйте и настройте

```bash
# Клонируйте репозиторий
git clone <your-repo-url> bybit-bot
cd bybit-bot

# Установите зависимости
pip install -e ".[dev]"

# Скопируйте конфиг
cp .env.example .env

# Отредактируйте .env
nano .env  # или vim, code, любой редактор
```

**Минимальная конфигурация в .env:**

```env
# Ваши API ключи от Bybit Testnet
BYBIT_API_KEY=ваш_api_key_здесь
BYBIT_API_SECRET=ваш_api_secret_здесь
BYBIT_TESTNET=true

# Профиль риска (рекомендуем начать с conservative)
RISK_PROFILE=conservative

# Telegram (если настроили)
TELEGRAM_ENABLED=true
TELEGRAM_BOT_TOKEN=ваш_telegram_token
TELEGRAM_CHAT_ID=ваш_chat_id
```

---

## Шаг 4: Запустите бота

### Вариант A: Локально (для тестов)

```bash
# Запустите PostgreSQL и Redis (нужен Docker)
docker compose up -d postgres redis

# Дождитесь инициализации (5-10 секунд)
sleep 10

# Запустите бота
python main.py
```

### Вариант B: Полный Docker (рекомендуется)

```bash
# Запустите всё в Docker
docker compose up -d

# Просмотр логов
docker compose logs -f bot

# Остановить: Ctrl+C, затем
docker compose down
```

---

## Шаг 5: Наблюдайте за работой

### В терминале

Вы увидите логи:
```
orchestrator_starting  session=20260208_143052
exchange_connected     testnet=True markets_count=300+
telegram_notifications_enabled chat_id=123456789
orchestrator_started
```

### В Telegram

Бот отправит уведомления:

**🟢 Открытие позиции:**
```
🟢 LONG BTCUSDT
Entry: 50000.5
Size: 0.02
SL: 49000 | TP: 52000
Strategy: ema_crossover
```

**✅ Закрытие с прибылью:**
```
✅ WIN BTCUSDT (long)
PnL: +100.45 USDT (+2.01%)
Entry: 50000 → Exit: 51000
Strategy: ema_crossover
```

**❌ Закрытие с убытком:**
```
❌ LOSS ETHUSDT (short)
PnL: -50.23 USDT (-1.67%)
Entry: 3000 → Exit: 3050
Strategy: mean_reversion
```

**🚨 Риск-событие:**
```
🚨 RISK ALERT
Reason: Max drawdown exceeded - trading halted
Current DD: 8.2%
Max DD Limit: 8.0%
```

---

## Шаг 6: Остановите и проанализируйте

```bash
# Остановите бота (Ctrl+C или)
docker compose down

# Проанализируйте сессию
python scripts/analyze_session.py journal.db

# Или если использовали Docker:
docker compose exec bot python scripts/analyze_session.py journal.db
```

**Пример отчёта:**
```
============================================================
SESSION REPORT: 20260208_143052
============================================================

--- TRADE STATISTICS ---
  total_trades: 15
  win_rate: 0.6000
  profit_factor: 1.8500
  avg_win: 125.30
  avg_loss: -68.15
  sharpe: 1.45
  total_pnl: 487.50

--- RISK SUMMARY ---
  total_signals: 42
  approved_signals: 28
  rejected_signals: 14
  rejection_rate: 0.3333

--- EQUITY CURVE ---
  start_equity: 10000.00
  end_equity: 10487.50
  max_drawdown: 0.0520
  return_pct: 0.0488
```

---

## Профили риска

Выберите профиль в `.env` через `RISK_PROFILE`:

### Conservative (консервативный) - рекомендуем для начала
- 1% риска на сделку
- Макс 5 позиций одновременно
- 2 убытка подряд → пауза 6 часов
- Макс drawdown: 8%
- Плечо: до 1.5x

### Moderate (умеренный)
- 2% риска на сделку
- Макс 10 позиций одновременно
- 3 убытка подряд → пауза 4 часа
- Макс drawdown: 15%
- Плечо: до 3x

### Aggressive (агрессивный) - только для опытных
- 4% риска на сделку
- Макс 15 позиций одновременно
- 5 убытков подряд → пауза 2 часа
- Макс drawdown: 25%
- Плечо: до 5x

---

## Troubleshooting

### Бот не запускается

```bash
# Проверьте логи
docker compose logs bot

# Проверьте .env
cat .env | grep BYBIT

# Проверьте подключение к Bybit
curl https://api-testnet.bybit.com/v5/market/time
```

### Telegram не работает

```bash
# Проверьте токен
curl https://api.telegram.org/bot<YOUR_TOKEN>/getMe

# Проверьте переменные
env | grep TELEGRAM
```

### PostgreSQL ошибки

```bash
# Пересоздайте БД
docker compose down -v
docker compose up -d
```

---

## Что дальше?

1. ✅ **Тестируйте 3-7 дней** на testnet с conservative профилем
2. ✅ **Анализируйте результаты** через `analyze_session.py`
3. ✅ **Корректируйте стратегии** если нужно
4. 🎯 **Переходите на mainnet** с минимальным капиталом

**Полная документация:**
- `DEPLOYMENT.md` - деплой на хостинг
- `README.md` - подробное описание
- `TODO_COMPLETED.md` - выполненные задачи

---

**Удачной торговли! 🎉**

*Remember: Это тестнет - деньги виртуальные. Экспериментируйте свободно!*
